# Event Management System

A fully functional Django Event Management System following the same project structure and conventions as the task-management reference project.

## Project Structure

```
event_management/
├── event_management/       ← Django project config
│   ├── settings.py         ← PostgreSQL + debug_toolbar configured
│   ├── urls.py
│   └── wsgi.py
├── events/                 ← Main Django app
│   ├── migrations/
│   ├── templates/events/
│   │   ├── base.html           ← Base with navbar, {% load static %}, output.css
│   │   ├── dashboard.html      ← Stats grid + interactive event listing
│   │   ├── event_list.html     ← Table view + search + filters
│   │   ├── event_detail.html
│   │   ├── event_form.html
│   │   ├── participant_list.html
│   │   ├── participant_detail.html
│   │   ├── participant_form.html
│   │   ├── category_list.html
│   │   ├── category_detail.html
│   │   ├── category_form.html
│   │   └── confirm_delete.html
│   ├── models.py           ← Category, Event, Participant
│   ├── views.py            ← All views with optimized queries
│   ├── forms.py            ← StyledFormMixin pattern
│   ├── urls.py
│   └── admin.py
├── static/
│   └── css/
│       ├── tailwind.css    ← Tailwind input file
│       ├── output.css      ← Compiled output (generated by npm)
│       └── style.css       ← Custom styles
├── manage.py
├── populate_db.py          ← Sample data generator
├── requirements.txt
├── package.json            ← Tailwind CSS via Node/npm
├── tailwind.config.js
└── postcss.config.js
```

## Setup Instructions

### 1. Install Python dependencies
```bash
pip install -r requirements.txt
```

### 2. Install Node.js dependencies & build Tailwind CSS
```bash
npm install
npm run build:tailwind
```

> During development, use `npm run watch:tailwind` to auto-rebuild on template changes.

### 3. Create PostgreSQL database
```sql
CREATE DATABASE event_management;
```
Update `settings.py` with your PostgreSQL credentials if different from defaults.

### 4. Run migrations
```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Populate sample data (optional)
```bash
python populate_db.py
```

### 6. Create superuser (optional)
```bash
python manage.py createsuperuser
```

### 7. Run the server
```bash
python manage.py runserver
```

Visit: http://127.0.0.1:8000/

---

## Key Features

| Feature | Details |
|---------|---------|
| **Models** | Category, Event (FK→Category), Participant (M2M→Event) |
| **CRUD** | Full Create/Read/Update/Delete for all 3 models |
| **Optimized Queries** | `select_related`, `prefetch_related`, `aggregate`, date range & category filters |
| **Search** | `icontains` on name & location via `request.GET` |
| **Dashboard** | Stats grid with clickable cards that dynamically update event list |
| **Forms** | `StyledFormMixin` for consistent Tailwind styling |
| **Debug Toolbar** | Enabled at `127.0.0.1` for query inspection |
| **Tailwind CSS** | Compiled locally via Node/npm (not CDN) |
